// Models Layer
// 📂 src/models/ → Defines the data structure and talks to the database.
// crop.model.js → schema for crops (name, type, price, farmerId).
// farmer.model.js → schema for farmer (name, location, contact).
// user.model.js → schema for login users (username, password, role).
// 👉 If using MongoDB → these will be Mongoose schemas.
// 👉 If using SQL → these will be Sequelize/Prisma models.
// Controllers call models to fetch/store/update data.