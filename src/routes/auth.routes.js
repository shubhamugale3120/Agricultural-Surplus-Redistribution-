// Routes Layer
// 📂 src/routes/ → Handles URLs/endpoints.
// Each file groups routes for a specific feature.

// crop.routes.js → defines endpoints for crops
// (GET /api/crops, POST /api/crops)

// farmer.routes.js → farmer-related APIs
// (GET /api/farmers)

// auth.routes.js → login/signup/logout

// 👉 Routes don’t contain logic, they just forward to controllers.